var x, y;
begin
    y := 3; /* Comment */
    x := y + 56;
    x >= y;
    x > y;
    y <= x;
    y < x;
    y odd x
    y = x;
    if (y = x)
    /* PLO */
    y, x, z;
    y + x;
    y * x;
    y / x;
    y - x;
end.
