#include "generator.h"

int generate(const char* mcodePath, token* tokens, symbol* symbolTable) {
    return 0;
}
int buildCode(char* instruction, int level, int address) {
    return 0;
}
void emit(FILE* mcode, char* code) {

}
